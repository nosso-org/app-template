const { colors } = require('tailwindcss/defaultTheme')

module.exports = {
    purge: [],
    theme: {            
        fontFamily: {
            sans: ['Rubik', 'sans-serif'],
        },
        extend: {            
        },
    },
    variants: {},
    plugins: [],
}
